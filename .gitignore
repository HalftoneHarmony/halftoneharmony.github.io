# 시스템 파일
.DS_Store

# 작업 문서 — 사이트에 배포하지 않음
*.md
*.command
Portfolio_2026-07-18/
스터디/

# 전략·사업 노트 — 사이트에 배포하지 않음
노트/

# 비즈니스 서식 (제안서·견적서·기획서·청구서) — 사이트에 배포하지 않음
서식/

# Claude 스킬 — 브랜드 서식 시스템 (사이트에 배포하지 않음)
# 공개 저장소에 올리고 싶으면 이 두 줄을 지우면 된다
.claude/
